{
  "name": "bnetdocs-static",
  "version": "0.1.0",
  "private": true,
  "description": "Static GitHub Pages build of bnetdocs.org",
  "scripts": {
    "build": "node scripts/build.js",
    "pagefind": "pagefind --site dist --output-path dist/pagefind"
  },
  "devDependencies": {
    "pagefind": "^1.3.0"
  }
}
